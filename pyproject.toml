[project]
name = "astrbot_plugin_kook_adapter"
version = "0.0.4"
description = "给astrbot增加kook的适配器"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.8.0",
    "websockets>=10.0",
]
